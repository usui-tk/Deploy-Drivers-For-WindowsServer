# Changelog

## 3.0.0 REV81 coordinated documentation closeout — 2026-08-22

- Records Claude's REV80 Cycle B closure and removes stale current-facing
  REV69-REV77 candidate/pending language from README, SPEC and TESTING.
- Declares bounded Gate 2G, the 69-file v3.0.0 public surface and exact REV77
  Server smoke as accepted/no-repeat while retaining earlier gates as
  historical evidence.
- Changes documentation only. The root script, contracts, schemas, reviewed
  data, generated public bytes, canonical path and accepted Evidence are
  unchanged; no Windows rerun or multi-hour survey is required.

## 3.0.0 REV78 coordinated qualification update — 2026-08-22

- Accepts the exact REV77 Windows Server / Windows PowerShell 5.1 Graphics
  smoke as 2/2 PASS with self-contained actual Server identity, closing the
  prior host-proof REVIEW.
- Keeps the Graphics source, schemas, data, and generated public tree
  unchanged; no Graphics rerun is required for the NPU-only REV78 correction.

## 3.0.0 REV77 execution-context Evidence correction — 2026-08-22

- Keeps the executable version at `3.0.0` and the canonical path unchanged.
- Corrects the REV76 self-contained Evidence gap by persisting actual host
  execution class, ProductType/role, OS identity, scopes, and collection state
  in every `run-context.json`.
- Adds the shared six-case classification/fallback self-test and advances the
  private Graphics Evidence schema from `1.0` to `1.1`.
- Leaves the accepted bounded-E2E generated `public/**` tree unchanged.

## 3.0.0 REV75 bounded Cycle B Gate 2G authorization — 2026-08-22

- Keeps the executable version and PowerShell source at `3.0.0` / REV74 bytes.
- Authorizes one Windows Client bounded E2E using one reviewed product group,
  one major generation, one selected artifact and a 2 GiB ceiling.
- Combines `PathSafety,Test` with acquisition-through-publication so no separate
  smoke or multi-hour all-track rerun is required.
- Requires a single Evidence ZIP containing the complete validated Graphics
  `snapshot/public/**`; Windows Server remains held.

## 3.0.0 REV74 PowerShell 5.1 cardinality correction — 2026-08-22

- Keeps the executable version at `3.0.0` and preserves product selection,
  bounded research, acquisition, extraction, signature and publication scope.
- Applies the same `@(...)` protection as NPU to the REV72
  self-contained-public manifest-row conditional assignment, preventing
  zero/one-item unrolling before `.Count` on Windows PowerShell 5.1.
- Repins the three-tool common-core contract to the corrected Graphics/NPU
  source identities; all 165 shared function bodies remain unchanged.
- Requires only `PathSafety,Test` before the later authorized bounded E2E; a
  multi-hour full rerun is not justified by this correction alone.

## 3.0.0 REV72 self-contained public Evidence correction — 2026-08-22

- Keeps the executable version at `3.0.0`.
- Includes the complete validated `public/**` tree beneath
  `snapshot/public/**` after successful current-run publication.
- Verifies live/snapshot path sets, byte identity and every publication
  manifest size/SHA-256 declaration before normal Evidence archive creation.
- Makes incomplete public snapshots fail closed through the existing emergency
  evidence path.
- Removes the operator-created public ZIP requirement without changing the
  bounded Graphics research or certificate scope.

## 3.0.0 REV70 transcript-hygiene correction — 2026-08-21

- Keeps the released tool identity at `3.0.0`; no version increment is made.
- Applies the three-tool-identical non-throwing negative-test path to the shared
  extraction-completeness assertion while preserving fail-closed production
  behavior.
- Prevents the expected `ExtractedWithErrors` fixture from appearing as a
  terminating error in Windows PowerShell 5.1 transcripts.
- Clarifies that the Graphics research runner does not enumerate or qualify the
  installed display adapter; AMD GPU presence is recorded as a host profile,
  not inferred from a successful static/publication run.

## 3.0.0 REV69 Cycle B candidate — 2026-08-21

- Synchronizes the three research-tool executable identities at `3.0.0`.
- Adds the explicit Windows PowerShell `#requires -Version 5.1` contract.
- Retains research and certificate-scope behavior while requiring fresh
  exact-source Cycle B qualification and generated-public replacement.
- The retained REV68 `public/**` tree is not hand-edited and is not a current
  `3.0.0` publication result.

## 1.1.2-dev REV65 documentation-authority refresh — 2026-08-21

- Updates README/SPEC/TESTING current status after the accepted bounded
  short-E2E, REV63 Windows Server `PathSafety,Test` smoke and REV64 launcher
  exclusion.
- Corrects obsolete future/pending language for the completed Client path
  correction, Signature/SignatureNative implementation and Server smoke while
  retaining their distinct qualification scopes.
- Adds an explicit downstream device-to-artifact build-script feedback contract
  without changing PowerShell source, data, schemas or generated `public/**`.

## 1.1.2-dev rev62 cumulative public-baseline correction — 2026-08-20

- Migrates imported historical release JSON to the current Canonical JSON byte contract before cumulative Build/publication.
- Replaces the staged release subtree from the authoritative runtime cumulative set instead of overlaying same-key files.
- Adds a normal Test regression for legacy CRLF/BOM baseline migration.
- Reports public validation category counts accurately; privacy-valid Canonical JSON failures are no longer mislabeled as privacy failures.
- Leaves Chipset, NPU, the 165-function common core, product selection, acquisition, extraction and signature scope unchanged.

## 1.1.2-dev rev60 public-null structure hardening — 2026-08-20

- Aligns the Graphics recursive public converter with the safe structural contract proven necessary by the REV59 NPU Windows failure.
- Preserves explicit null-valued properties and null array items while continuing to remove private path fields and rejected host-path strings.
- Extends the normal canonical-publication self-test with null-property/null-array preservation plus nested `ArchivePath` removal.
- Does not change product selection, acquisition, extraction, signature scope or accepted long-run research results.

## 1.1.2-dev rev59 public-path audit hardening — 2026-08-20

- Adds `ArchivePath` and the complete shared path-property classification to both canonical and public-object privacy conversion.
- Converges forbidden-pattern detection and decoded JSON scalar traversal with Chipset and NPU.
- Extends the normal canonical-publication self-test with a nested `PathSafety.ArchivePath` negative fixture while preserving vendor selector tokens.

## 1.1.2-dev rev58 cross-tool path-safety correction — 2026-08-20

- Routes the existing short artifact/container aliases through the shared `Get-AmdShortExtractionPath` constructor so startup prediction and runtime layout have one authority.
- Makes incomplete extraction a terminating Extract failure and adds an explicit signature-stage completeness assertion.
- Keeps product selection, graphics analysis-surface detection and certificate-target semantics unchanged.



## 1.1.2-dev rev57 common-core convergence preview — 2026-08-20

- Generalizes the accepted path-safety kernel so the same implementation can be embedded in Chipset and NPU while retaining Graphics-specific policy values.
- Adds bootstrap-fatal emergency evidence parity with NPU, a current three-tool common-core hash contract, and shared ordinal ordering helpers.
- Uses ordinal file ordering for publication-manifest generation without changing product/category selection or certificate-target semantics.

## rev53 Windows PowerShell 5.1 empty native-output hotfix — 2026-08-20

- Fixes the `PathSafety` SUBST diagnostic failure when `subst.exe` succeeds
  with zero output on Windows PowerShell 5.1.
- Normalizes PS5.1's `$null` representation of an empty emitted byte array to
  a real zero-byte payload before SHA-256 calculation; the SUBST check remains
  fail-closed and is not bypassed.
- Adds a normal `Test`-stage regression assertion for the canonical SHA-256 of
  an empty payload.

## rev52 interruption and Evidence portability hardening — 2026-08-19

- Ports the Chipset fail-closed `RUNNING` to `INTERRUPTED` tracked-stage behavior.
- Reports interrupted runs as `Interrupted` with exit code `130`; empty bootstrap stage state is non-PASS.
- Advances the private assessment schema to 1.3 for `Interrupted`, `INTERRUPTED` and `NOT_ASSESSED` values.
- Creates a normal Fatal Evidence ZIP for safe-root bootstrap/argument failures that occur before the ordinary evidence session; an unsafe PathSafety root remains a strict no-write block.
- Adds complete stage lifecycle diagnostic events and normalizes private Evidence manifest paths to `/`.
- Does not authorize or require another 12-hour full Graphics run for this lifecycle-only correction.

## rev51 Canonical JSON performance and evidence recovery — 2026-08-19

- Uses the byte-compatible accelerated Canonical JSON runtime shared by all three tools.
- Skips the 121 MiB public-baseline copy for `PathSafety`/`Test`-only execution.
- Adds immediate bootstrap and per-Test-operation elapsed output.
- Adds verified emergency ZIP/SHA-256 fallback with raw evidence retention.

## rev50 three-tool Canonical JSON convergence — 2026-08-19

- Adds the shared cross-runtime Canonical JSON writer/parser/file/hash helpers.
- Replaces compact-whitespace publication validation with canonical byte
  equality and preserves date-looking strings across PS 5.1/7.
- Adds a fixed-vector cross-runtime self-test without requiring a full run.

## 1.1.2-dev — 2026-08-18

- Adds an automatic, non-bypassable Windows `PathSafety` stage before Evidence
  output and AMD network access: tool root maximum 100, safe full-path maximum
  240, AMD relative-path reserve 120, tool-folder-only storage, UNC/reparse
  qualification block, and real 200/220/240 filesystem probes.
- Records `LongPathsEnabled` as diagnostic only and adds 7-Zip and required
  SignTool short-path probes.
- Replaces future runtime names with short acquisition (`private/a`), extraction
  (`work/x/aNNNN/cNNNN`), native alias (`work/n/fNNNNNN.ext`) and Evidence
  (`private/evidence/runs`) layouts while preserving original names/URLs/SHA/provenance in
  mapping evidence.
- Adds structured 7-Zip listing preflight before outer and nested extraction;
  unsafe, traversal or over-240 predicted paths fail closed before extraction.
- Routes every Windows-native Authenticode/catalog/SignTool input through a
  SHA-256-verified byte-identical short alias, removing the 260–357 character
  SignTool failure mode observed in the first Windows Client run.
- Adds `SignatureNative`, which validates and reuses the existing static
  signature analysis and reruns only native evidence. It does not repeat the
  approximately 11.5-hour static CMS analysis.
- Adds path-safety, artifact-path, extraction-path and native-alias schemas and
  expands built-in common/signature self-tests from 16 to 19.

## 1.1.1-dev — 2026-08-17

- Closed the Windows Client false-qualification boundary found by the independent
  rev45 audit: qualification now requires the complete unfiltered three-generation
  product-driven research selection and a non-publishing AMD-only acquisition.
- Persisted the requested generation count and product-group filter in the
  certificate verification plan and revalidates both during Signature assessment.
- Added a negative boundary self-test and removed the duplicate INF integer helper.

## 1.1.0-dev — 2026-08-17

Graphics certificate targeting, shared signature engine and common hardening
implementation candidate.

- Preserves newest-three-major-generation ordinary research per stable
  `SelectionTrackKey`.
- Adds a deterministic certificate plan owned by `Select`: newest selected
  major generation per product-group + OS + package-family track.
- Resolves URL targets after `Acquire` by immutable outer-installer SHA-256
  and retains all many-to-one provenance.
- Adds `Signature` between `Inspect` and `Build`, including PE
  `WIN_CERTIFICATE`, CMS/PKCS#7, nested signatures, timestamp tokens, X.509,
  Authenticode digest comparison, Windows catalog APIs, SDK/WDK identity and
  locale-neutral SignTool profiles.
- De-duplicates static candidate parsing globally by file SHA-256 while
  retaining artifact-contextual catalog/kernel association.
- Ports sequential HTTP, bounded retry/backoff, `Retry-After`, partial
  response rejection, `.partial` atomic completion, byte conservation and
  structured per-attempt evidence.
- Adds redacted JSONL diagnostics/failure snapshots and PowerShell 5.1
  cardinality/source-contract tests.
- Adds certificate-plan, static-signature and native-signature schemas and
  publishes only host-neutral signature/toolchain summaries.
- Local gates passed on PowerShell 7.6.5: parser, Test stage, retained
  38/95/23 selection fixture, narrowed 38/9/57 certificate fixture, offline
  signed-PE Signature integration, schema validation, privacy validation and
  publication-manifest integrity.
- Windows Client Gate A and Windows Server qualification remain unperformed.

## rev44 enhancement planning record — 2026-08-17 (documentation only)

- Adds a non-normative cross-tool assessment and future Graphics
  signature/common-hardening plan under `authored/**`.
- Preserves the current newest-three-major-generation research contract and
  defines the future deep-certificate target as only the newest selected
  generation per stable product-group + OS + package-family track.
- Records layered URL, outer-installer SHA-256 and extracted-file SHA-256
  de-duplication with complete product/track provenance retention.
- Identifies common Chipset/NPU signature, toolchain, transport, diagnostic and
  PowerShell 5.1 hardening to complete before a user rerun.
- Adds only documentation notices to README, SPEC and TESTING. Graphics remains
  version `1.0.0`; no PowerShell, schema, reviewed data or generated `public/**`
  bytes change, and Signature remains unimplemented pending separate approval.

## Authored/generated separation — 2026-08-14 (documentation and repository layout only)

`reports/` held both the authored hardening and qualification records and the
locations the toolkit writes generated reports to, separated only by a list of
file names in `.gitignore`.

- Separates authored records from script-generated output by directory. The
  authored design/qualification narratives move from `reports/` to a new
  `authored/` directory as a pure rename; no file content changes.
- Replaces the hand-maintained generated-file name list in `.gitignore` with a
  whole-directory rule. `reports/**` is now ignored in its entirety apart from
  its `.gitkeep`, so a newly generated report can no longer become committable
  by being absent from a list.
- Adopts the repository-wide `tools/` layer convention: `authored/**` and
  `public/**` are committed; `inventory/**`, `private/**`, `work/**` and
  `reports/**` are runtime staging carrying one tracked `.gitkeep` each.
- Updates the internal documentation references and the normative statements
  that previously directed new one-off narratives into `reports/**`.
- No PowerShell change, no toolkit version change, and no generated `public/**`
  byte changes.

## 1.0.0 release-candidate publication hardening — 2026-08-12

- Reorganizes release documentation without changing PowerShell runtime logic or generated research data: the top-level contract is reduced to `README.md`, `SPEC.md`, `TESTING.md`, `PUBLICATION-POLICY.md`, `RESEARCH-NOTES.md`, `CHANGELOG.md`, and `THIRD-PARTY-NOTICES.md`; historical design/qualification notes move under `reports/**`, and the byte-identical curated/rerun qualification duplicate is retained once.
- Consolidates reverse-engineering/deployment knowledge into `RESEARCH-NOTES.md` and adds `reports/README.md` as the historical evidence index.
- Implements Claude final-audit change request: every generated `public/**` Markdown file is deterministically emitted as UTF-8 without BOM with LF-only line endings, matching the chipset toolkit and repository Markdown convention. Runtime report Markdown records `GenerationMode=MarkdownLfNoBomFromRuntime`; toolkit-authored/preserved Markdown records an explicit LF/no-BOM generation mode.
- Keeps canonical JSON and CSV publication byte-faithful. JSON remains compact at canonical generation time and is never publication-time reserialized or line-ending normalized.
- Extends publication validation with a fail-closed Markdown encoding/EOL check and adds `PublicMarkdownPublicationSelfTest`.
- Closes audit request N-01 by including exact runtime `inventory/**` and `reports/**` staging under private Evidence `snapshot/inventory/**` and `snapshot/reports/**`, while retaining `snapshot/runtime-inventory/**` for compatibility. This makes manifest `SourceRelativePath` / `SourceSha256` independently verifiable from the Evidence ZIP.
- Toolkit version remains `1.0.0`; the prior Windows-generated v1.0.0 dataset is superseded and must be fully regenerated after this source change.

## 0.8.5-publication-dev — 2026-08-12

- Fixes the Windows PowerShell 5.1 full-run publication failure observed with 23 selected artifacts: every canonical artifact inherited a multi-line 7-Zip extraction `Log` containing the machine-local toolkit/work path, so decoded-scalar privacy validation correctly rejected all 23 files.
- Hardens `ConvertTo-AmdPortableEvidenceObject` so any string containing a configured private/runtime path pattern is omitted regardless of where the path occurs in the string; diagnostic `Log`/transcript/exception fields are also excluded unconditionally from canonical public-bound records.
- Adds a self-test using a multi-line 7-Zip-style log to prove that host paths are removed while vendor selectors such as `/SETFILTERUSB` remain byte-faithful.
- No INF-selection, WDF, product-discovery, acquisition, extraction, or Windows Server compatibility-analysis semantics are changed. A new Windows full run is required before v1.0.0 promotion.

## 0.8.4-publication-dev — 2026-08-12

- Applies the Claude pre-commit scope remediation across every genuine script-level parameter reference inside functions: `SkipPublicExport`, both `EmitDetailedDeviceMatrix` reads, `LocalInstallerPath`, `FullHistoricalResearch`, `ReleaseVersion`, and `ReleaseKey` now use explicit `$script:` scope. The `Add-Candidate` `ReleaseVersion` closure remains intentionally local to its enclosing function.
- Aligns generated-JSON/publication handling with the hardened AMD Chipset research toolkit: canonical JSON is compact when generated and publication no longer performs a separate JSON minification or parse/reserialization pass.
- Adds `Get-AmdCollectionItems` and routes generated-JSON collection reads through it so Windows PowerShell 5.1 `{ value: [...], Count: n }` wrappers are rehydrated at consumption time without rewriting canonical Raw JSON.
- Changes JSON privacy validation from raw serialized-text matching to decoded scalar inspection, preventing escaped Windows paths from bypassing the gate while preserving vendor tokens such as `/SETFILTERUSB` and `/info.xml`.
- Advances publication manifest/validation schemas to `amd-graphics-publication-manifest/1.3` and `amd-graphics-publication-validation/1.3`; manifest metadata now distinguishes manifested payload count from total public files and records total/largest payload size metrics.
- Makes `selection-plan.json` conditional for dataset consistency so a fresh local-installer-only run can publish a valid one-artifact dataset without inheriting a historical public baseline.
- Adds compact-JSON, decoded-scalar privacy and PowerShell 5.1 wrapper self-tests. PowerShell 7.6.4/Linux regression with the qualified Adrenalin 26.7.1 installer passes all five implicit local stages, 40 INF / 19 KMDF / 2 UMDF / 0 parse failures, publication validation and manifest integrity with exit code 0.
- This is a development qualification checkpoint, not the v1.0.0 GitHub release candidate; a fresh Windows PowerShell 5.1 user run remains required.

## 0.8.2 — 2026-08-11

- Documentation knowledge consolidation: expands `README.md` into a reverse-engineering/deployment-reference guide covering product-group/artifact selection, direct 7z SFX observations, InstallManifest evidence, canonical INF selector topology, native Server control behavior, Win11-vs-Server binary/HWID differences, WDF scope, provenance/signing boundaries, and recommended future self-signed build/deployment decision flow. No PowerShell runtime logic or historical canonical evidence is changed by this documentation-only alignment.
- Validated a complete Windows PowerShell 5.1 product-driven run: 21 curated product groups, 23 selected/acquired/extracted/inspected artifacts, 482 current INF records, 27 cumulative artifacts, 551 cumulative drivers and 2,204 Windows Server analysis rows.
- Hardens `public/publication-manifest.json` for GitHub Actions and Linux validation: `RelativePath` is now always POSIX-relative (`/`) independent of the host OS; backslashes, absolute paths, unsafe path segments, duplicates, missing files, size mismatches, SHA-256 mismatches and unmanifested public files fail publication before atomic promotion.
- Adds public cross-file consistency validation between `run-summary.json`, `selection-plan.json`, `all-releases-summary.json`, `inf-topology.json` and the Windows Server aggregate.
- Adds `PublicationManifestSelfTest` to the Test stage and bumps publication manifest/validation schemas to 1.1.
- Preserves the public/private trust boundary; private Evidence remains excluded from automated repository commits.


## 0.8.1 — 2026-08-11

- Fixes a Windows live-run failure where two curated Ryzen PRO support pages returned transient HTTP `403` responses and the default fail-closed policy blocked the entire three-generation baseline.
- Adds bounded product-page request pacing and retry/backoff for HTTP `403`, `408`, `425`, `429`, selected `5xx`, and transport failures.
- Adds same-product-group alternate AMD official support pages for the Ryzen PRO 5000 and Ryzen PRO 8000 curated representatives while retaining the original configured page as primary provenance.
- Curated processor/iGPU groups now query Previous Drivers even when the latest page fails, preventing a latest-page transport failure from being misreported as a product with no graphics history.
- Adds latest/previous requested/effective URL, primary/alternate source, retry attempts, recovery counts and cache sidecar provenance to the product-driver mapping (`1.2`).
- Adds `ProductMetadataFetchPolicySelfTest`.
- Downstream assessment items now emit `SKIP` when Acquire was blocked instead of generating duplicate missing-file `REVIEW` findings.
- A non-passing core run no longer updates `public/**`; the last validated public repository surface is preserved unchanged.
- Updates curated product-group catalog/schema to `2.2` / catalog revision `2026-08-11.2`.
- Adds documentation and testing requirements for transient AMD metadata failures, fallback provenance and publication eligibility.


## 0.8.0 — 2026-08-11

- Documentation alignment: adds `TESTING.md` and expands README/SPEC/PUBLICATION-POLICY/public README with the explicit GitHub Actions allow-list contract, public/private classification matrix, publication fail-closed behavior, manifest verification, partial-run preservation, fresh-checkout baseline seeding, and generated-output staged-path checks.
- Clarifies that `public/**` is the only **generated** auto-commit surface; static script/schema/curated-data/documentation changes remain separately reviewed repository source changes.
- Introduces an explicit generated-output privacy boundary: repository-safe generated data/reports are published only under `public/**`; detailed host/debug evidence moves to `private/evidence/**` by default.
- Adds `-PublicOutputRoot` and `-SkipPublicExport`. `-EvidenceOutputRoot` continues to control the private evidence location.
- Adds privacy-aware publication staging and validation. Absolute local paths, host PowerShell/OS fields, Evidence locations, HTML-cache paths and other execution-context fields are excluded from the public surface. Publication failure leaves the previous public baseline untouched and is surfaced as `PublicRepositorySurface=REVIEW`.
- Adds `public/publication-manifest.json`, `public/publication-validation.json`, repository-safe `public/run-summary.json`, and `public/run-report.md` for GitHub Actions automation.
- Publishes portable product catalog/mapping/selection data, canonical per-artifact JSON, aggregate inventory/Server views and generated release reports while keeping acquisition/extraction/debug artifacts private.
- Adds runtime-baseline seeding from `public/inventory/**`, allowing tracked canonical data to live in the public surface while `inventory/**` remains ignored runtime staging.
- Changes default HTML/download/transcript/extraction evidence paths from `evidence/**` to `private/evidence/**`.
- Retains the 0.7.4 Windows PowerShell 5.1-qualified 27-artifact / 551-driver / 2,204-Server-row baseline as the initial public canonical dataset.

## 0.7.4 — 2026-08-11

- Confirms the user-supplied Windows PowerShell 5.1 full run `AmdGraphicsDriverResearchEvidence_20260811-080459_Windows.zip` as a clean `Pass / exit 0`: all eight stages passed, 23 selected/acquired/extracted/inspected artifacts remained identical by ArtifactKey, 482 INF parsed with zero failures, shard integrity passed, and cumulative Build integrity remained 27 artifacts / 551 drivers / 2,204 Server rows.
- Removes misleading transcript noise from the RunAssessment self-test. Unsupported future driver-package index schemas now return a structured `REVIEW` assessment instead of throwing an expected terminating error that PowerShell transcripts displayed as `PS>終了エラー`.
- Adds `ArtifactPipelineConsistency` to the final assessment. It cross-checks Selection -> Acquisition -> Extraction -> Inspect ArtifactKey sets and verifies acquisition SHA-256 values against extraction installer SHA-256 values.
- Reports estimated versus actual acquired bytes as provenance rather than requiring exact size equality; AMD-published estimates may legitimately differ from downloaded file size.
- Extends `RunAssessmentSelfTest` to cover the non-throwing unsupported-schema contract and a valid end-to-end artifact-chain assessment.
- Keeps existing shard-integrity, inspection-assessment consistency and cumulative Build-integrity gates unchanged.
- Promotes the fresh Windows PowerShell 5.1 full-run canonical outputs into the development package: 27 retained canonical artifacts, 551 cumulative driver records and 2,204 Windows Server analysis rows. Run-scoped acquisition/extraction paths remain Evidence-only and are not committed as canonical data.

## 0.7.3 — 2026-08-11

- Fixes the Windows PowerShell 5.1 final run report exposed by `AmdGraphicsDriverResearchEvidence_20260811-064906_Windows.zip`: all eight stages passed, but ProductSelection incorrectly emitted a PowerShell `if` command error and InfInspection incorrectly rejected the current `amd-graphics-driver-packages-index/3.1` schema.
- Replaces inline optional-property `if` expressions in ProductSelection reporting with typed helper reads and structural count validation.
- Centralizes supported driver-package index schemas (`3.0`, `3.1`) so assessment, shard-integrity validation and Build cannot silently drift apart.
- Adds pure `RunAssessmentSelfTest` coverage for valid selection-plan notes, current 3.1 indexes, historical 3.0 indexes and unsupported-schema rejection.
- Adds `InspectionAssessmentConsistency` to the final report, cross-checking artifact/driver/parse-failure counts against `driver-packages-integrity.json`.
- Re-evaluates the supplied successful Windows run as Pass / exit 0: 23 current artifacts, 482 INF, zero parse failures, shard integrity Pass, cumulative 27 artifacts / 551 drivers / 2,204 Server rows, aggregate integrity Pass.

## 0.7.2 — 2026-08-11

- Confirms the 0.7.1 memory-bounded storage model on the user-supplied Windows PowerShell 5.1 full run: 21 curated groups, 23/23 acquired and extracted artifacts, 482 INF, zero parse failures, Build PASS in 45.26 seconds, overall PASS / exit 0.
- Adds per-artifact detail JSON SHA-256 and byte-size fields and advances the Inspect index to `amd-graphics-driver-packages-index/3.1`; Build remains able to consume historical 3.0 indexes.
- Adds `driver-packages-integrity.json` and validates every index/detail relation, ArtifactKey/ReleaseKey, counts, parse failures, WDF declaration counts, detail size and SHA-256 before Build.
- Adds `build-integrity.json` and fails Build if cumulative summary/topology/Windows Server aggregates disagree on artifact coverage, driver totals, or expected four-profile server row counts.
- Fixes a cumulative aggregate migration defect exposed by the successful Windows run: retained 0.7.0-era canonical artifacts were present in `all-releases-summary.json` but omitted from the new lightweight topology and Server aggregate views. Historical 2.1 Server rows are now normalized in memory without rewriting historical evidence.
- Uses the retained all-releases summary as the lightweight source for historical topology references when migrating from `inf-topology-collection/1.2`, preventing `ArtifactCount`/`DriverCount` contradictions.
- Final assessment now reports shard integrity and Build aggregate integrity, and ProductSelection detail includes OS-track evidence-conflict and ArtifactRole-transition counts.
- Clarifies Build console output by separating current-run artifact/INF counts from cumulative artifact/driver/Server-row counts.

## 0.7.1 — 2026-08-11

- Fixes a Windows PowerShell 5.1 `System.OutOfMemoryException` observed after a successful 23-artifact / 482-INF live run. The 0.7.0 Inspect output had grown into an approximately 913 MiB pretty-printed monolithic `driver-packages.json`, and Build attempted to deserialize/embed/serialize that dense object again.
- Replaces the monolithic Inspect output with `amd-graphics-driver-packages-index/3.0` plus one compact `amd-graphics-driver-packages-artifact/1.0` detail JSON per ArtifactKey.
- Build processes current artifact detail JSON one at a time and no longer embeds all driver packages in the top-level research inventory.
- Avoids reparsing dense historical canonical JSON solely for aggregate regeneration; compatible lightweight aggregate indexes/rows are reused instead.
- Emits compact JSON and forces an artifact-boundary garbage collection on both Windows PowerShell and PowerShell 7.
- Makes full per-HWID device/server aggregate expansion opt-in with `-EmitDetailedDeviceMatrix`; the default device/server CSV is an INF × Server summary.
- Clears run-scoped `inventory/driver-packages-artifacts/` before new deep-analysis acquisition so stale intermediate detail records cannot leak across runs.
- Updates final run assessment for the new driver-package index schema and preserves compatibility with historical monolithic Evidence.
- Adds schemas for the new driver-package index and per-artifact detail envelopes.
- Hardens Build against empty release references and incompatible legacy aggregate rows encountered during migration/replay.
- Regression replay of the Windows Evidence dataset completes Build for 23 artifacts / 482 INF / 1,928 INF×Server rows in about 27 seconds on PowerShell 7.6.4, versus the 0.7.0 Windows Build OOM.

## 0.7.0 — 2026-08-11

Curated product-group research scope and product-metadata coverage hardening.

- Replaces the non-functional sitemap-dependent no-switch discovery path with a versioned `CuratedProductGroups` catalog based on AMD official support pages.
- Catalog version `2026-08-11.1` declares 21 representative current/recent product groups across Radeon RX, Radeon PRO/AI PRO, Ryzen and Ryzen PRO integrated Radeon graphics, while explicitly listing legacy/mobile/embedded exclusions.
- Keeps the catalog honest: `CanClaimFullProductCatalog=false` and `CoveragePolicy=RepresentativeProductPerProductGroup`; the tool does not claim to enumerate every AMD model.
- Keeps three-major-generation selection dynamic from each representative group's latest/previous AMD pages and globally deduplicates identical AMD EXE URLs.
- Adds product-metadata coverage contract `amd-graphics-product-driver-mapping/1.1` with `MetadataCompleteness`, fetch-failure counts, no-driver-entry counts and `CanProceedToAcquire`.
- Blocks default Acquire if any curated representative current/previous page cannot be evaluated, preventing partial three-generation research from silently proceeding.
- Propagates `ProductGroupDisplayName` / `GraphicsKind` into product/group mapping evidence and records catalog coverage policy/default exclusions in `products.json`.
- Adds `product-group-catalog.schema.json` and updates product catalog/mapping/selection schemas.
- Regression: 21-group cached fixture completes ProductMetadata/Select with 21 products, 22 stable tracks, 66 track-generation selections and 12 globally deduplicated synthetic EXE URLs; one missing Previous Drivers page correctly produces Partial metadata, Acquire BLOCKED and Extract/Inspect/Build SKIPPED.
- Real-artifact regression remains PASS with PRO 26.Q1 Windows 11 Vega/Polaris: Test/Acquire/Extract/Inspect/Build PASS, 14 INF, parse failures 0.

## 0.6.2 — 2026-08-11

Windows rerun correctness and stable product-track selection hardening.

- Reviews the user-supplied Windows PowerShell 5.1 evidence run `AmdGraphicsDriverResearchEvidence_20260811-013548_Windows.zip` and treats it as the authoritative regression source for this fix.
- Fixes pipeline orchestration: intentional SeedOnlyFallback acquisition refusal is now `BLOCKED`, and every later selected dependent stage is `SKIPPED`. Extract/Inspect no longer emit secondary missing-file failures and Build can no longer report PASS from retained historical canonical records after an upstream block/failure.
- Clears only run-scoped deep-analysis outputs (`acquisition.json`, `extraction.json`, `driver-packages.json`, embedded installer metadata, current-run inventory) before Acquire while preserving canonical `inventory/releases/**` history.
- Separates stable `SelectionTrackKey = ProductGroupKey|OperatingSystemTrack|PackageFamily` from raw `DriverTrackKey = ...|ArtifactRole`. ArtifactRole remains provenance but no longer fragments a product lineage across historical releases.
- Re-evaluating the actual 0.6.1 Windows mapping changes the representative seed plan from 31 track-generation selections / 27 unique EXEs / about 24.66 GiB to 8 stable tracks / 20 track-generation selections / 17 unique EXEs / about 15.49 GiB.
- Preserves ArtifactRole transitions as explicit selection-plan evidence and retains all sibling EXEs when the newest release in a generation exposes multiple distinct artifacts.
- Updates selection-plan schema to `amd-graphics-product-selection-plan/1.2` and stage-results schema tag to `amd-graphics-driver-research-stage-results/1.1`.
- Distinguishes stable track-generation count from artifact-selection count (`TrackGenerationSelectionCount` vs `ArtifactSelectionCount`) so sibling EXEs in one selected release do not corrupt planning metrics.
- Extends ProductDrivenSelectionSelfTest to validate stable-track semantics.
- Keeps `SeedOnlyFallback` fail-closed by default; `-AllowSeedOnlyProductDiscovery` remains an explicit representative-seed research opt-in.

## 0.6.1 — 2026-08-11

Windows live product-driven qualification hardening.

- Reviews two user-supplied Windows PowerShell 5.1 evidence runs: preview `ProductDiscover,ProductMetadata,Select` completed in 1.45s; full product-driven run completed in 31m32.6s with all 8 stages PASS / exit 0, 13 downloaded artifacts and 230 parsed INF records.
- Detects that AMD live sitemap discovery contributed zero support product pages in the observed Windows run; the catalog contained only the 3 representative seed products. Product catalog output now records `SitemapBackedBestEffort`, `SeedOnlyFallback`, or `NoUsableProducts` instead of implying sitemap-backed completeness.
- Blocks default product-driven Acquire when discovery is `SeedOnlyFallback`; operators may review the plan, add explicit product pages/groups, or opt in with `-AllowSeedOnlyProductDiscovery`. This prevents representative seeds from being mistaken for a complete AMD product catalog.
- Adds filename-based Windows OS-track evidence for server-specific installer names (`winsvr2016/2019/2022/2025`) and records page/filename evidence plus conflicts. A live evidence defect that mapped `amd-software-pro-edition-25.q3.1-winsvr2025-rdna.exe` under the Windows Server 2016 track is covered by regression self-test and corrected by filename override.
- Hardens product-name extraction to derive the product name from the page `<title>` rather than a greedy whole-page plain-text match.
- Adds `-MaximumEstimatedDownloadGiB` (default 32 GiB, 0 disables) alongside the artifact-count guard; selection plan records both limits and the estimated download volume.
- Extends product-driven self-tests with OS-track conflict and product-title parsing cases; PowerShell AST parse errors remain zero.


## 0.6.0 — 2026-08-11

Product-driven discovery and bounded three-major-generation selection milestone.

- Changes the no-switch default from bulk release-driven acquisition to `Test -> ProductDiscover -> ProductMetadata -> Select -> Acquire -> Extract -> Inspect -> Build`.
- Adds AMD product support-page discovery for both `graphics/...` products and `processors/...` products that may expose integrated Radeon graphics drivers.
- Adds `ProductKey`, `ProductGroupKey`, product-driver mapping and product-group inventory outputs.
- Adds product-driver tracks keyed by product group, operating-system track, package family and artifact role so client Adrenalin, client PRO and Windows Server PRO mappings are not collapsed.
- Adds latest-N major-generation selection (`-MajorGenerationCount`, default 3): each track keeps its newest available three major generations and the newest release within each generation.
- Adds global direct-AMD-EXE URL deduplication so many product pages referencing the same installer cause one acquisition while retaining all product/track provenance.
- Adds `-ProductGroupKey`, `-AdditionalProductPageUrl`, `-MaximumSelectedArtifactCount` (default 32) and explicit `-FullHistoricalResearch`.
- Preserves legacy `Discover` / `Metadata` and explicit `-ReleaseVersion` / `-ReleaseKey` research; full historical acquisition is no longer implicit.
- Excludes Auto-Detect/minimal-setup and unrelated preview packages from canonical full-driver selection.
- Adds `product-catalog`, `product-driver-mapping` and `product-selection-plan` schemas plus representative AMD product seeds.
- Adds product-driven self-tests covering dGPU support URLs, processor/iGPU support URLs, Adrenalin/PRO installer parsing and Auto-Detect exclusion.
- Offline product-selection fixture: 3 product groups, 12 track-generation selections, 11 unique AMD EXE URLs after global deduplication.
- Real local regression: PRO 26.Q1 Windows 11 Vega/Polaris completed `Test,Acquire,Extract,Inspect,Build` with 14 INF, zero parse failures and PASS / exit 0 using the 0.6.0 script.
- Live AMD end-to-end product crawling remains a Windows/network qualification gate because the development container did not have outbound AMD access.

## 0.5.0 — 2026-08-11

- Qualified PRO Edition 26.Q1 Windows 11 Vega/Polaris with the unchanged 0.5.0 script: 14 INF, zero parse failures, 10/10 manifest payload resolution, `ArtifactRole=PolarisVega`.
- Completed direct static sibling comparison against the AMD-published Windows Server 2022 Vega/Polaris package: Server Display HWIDs are a 314-ID subset of the 343-ID Windows 11 topology; 29 client IDs are not present in the Server INF.
- Confirmed the three common component INFs (`amdafd.inf`, `amdfendr.inf`, `amdocl.inf`) are byte-identical, while the primary Display INF and observed `amdkmdag.sys` payload differ despite the same Display Driver version `31.0.21924.61`; Server additionally exposes `amdgpuv.sys`.
- Corrected the normalized qualified-sample transcription for the Server 2022 Vega/Polaris artifact from `InfParseFailureCount=5` to the evidence-supported value `0`; historical canonical/evidence files are not rewritten.
- Cumulative development dataset after Set 5: 5 canonical artifacts, 108 INF topology records, 432 INF×Server rows, 22,297 device/HWID×Server rows.

- Forward-ported the shared INF semantic contract from the chipset research work onto the qualified 0.4.3 Graphics baseline; no chipset artifact or chipset script is modified by this package.
- Removed the fixed-prefix filter from Models identifier preservation. Every non-empty Models identifier is retained as source evidence.
- Added `Identifier` and `CompatibleIdentifiers` taxonomy objects and retained `HardwareId` / `CompatibleIds` compatibility aliases.
- Added `amd-inf-semantic-contract/1.0`, `amd-inf-identifier-taxonomy/1.0`, and migrated newly generated topology to `amd-inf-topology/1.1`; retained the legacy 1.0 schema for historical validation.
- Added canonical TargetOSVersion and Windows Server profile aliases without removing 0.4.x fields.
- Added `CanonicalStaticAssessment`, `RuntimeCompatibilityProven=false`, and explicit `WdfScope=InfWideConservative`.
- Added identifier-taxonomy self-tests, native Server 20348 selector regression, and explicit WDF-scope regression while retaining Graphics MultiArtifact identity tests.
- Explicitly did not port chipset-specific DevID.xml / SETxxx / MSI selector reverse engineering into Graphics.



## 0.4.3 — 2026-08-10

PRO Windows Server identity and native-Server targeting qualification milestone.

- Qualifies `amd-software-pro-edition-26.q1-winsvr2022-vega-polaris.exe` without launching AMD setup.
- Fixes PRO sibling ReleaseKey normalization: all PRO 26.Q1 sibling artifacts now retain `Branch=MultiArtifact`; RDNA / Polaris-Vega / Windows Server distinctions remain in `ArtifactRole`.
- Adds a Graphics identity self-test covering PRO RDNA, PRO Vega/Polaris, PRO Windows Server Vega/Polaris and Adrenalin Vega/Polaris classification.
- Real Server package observation: 5 INF, 0 parse failures; manifest 3/3 payloads resolved; primary Display driver `31.0.21924.61`.
- Primary Server Display INF contains populated `NTamd64.10.0.2..20348` and `NTamd64.10.0.3..20348` sections and empty less-specific Server/DC sections, yielding native INF selection for Server 2022/2025 and explicit exclusion for Server 2016/2019.
- Preserves the Embedded layer separately: the manifest describes `Display driver for Windows Server 2022` and records raw `OSCheck=*-*-10.0.20348.0-Yes-*-Yes`; the tool does not reinterpret this AMD-proprietary condition as runtime compatibility.
- Cumulative canonical dataset after qualification: 4 artifacts, 94 INF topology records, 376 INF×Server rows.

## 0.4.2 — 2026-08-10

PRO Edition 26.Q1 Windows 11 RDNA qualification and Build scalability hardening.

- Qualifies `amd-software-pro-edition-26.q1-win11-rdna.exe`: direct 7z SFX, 39 INF, 0 parse failures, 17 KMDF-declaring INF and 2 UMDF-declaring INF.
- Records 31/31 InstallManifest payload matches (27 DRIVER / 3 MSI / 1 EXEC_CMD); primary display driver `32.0.21041.1000` in `U0200269.INF`.
- Preserves `PackageFamily=ProEdition`, `Branch=MultiArtifact`, `ArtifactRole=RDNA` and a distinct ArtifactKey suitable for later sibling PRO artifacts.
- Primary Display INF uses populated `NTamd64.10.0.1..19044`; Server 2016/2019 are not applicable by build/product targeting, while Server 2022/2025 are `PATCH_CANDIDATE` only under the virtual ProductType 1→3 projection.
- Removes redundant run-scoped `inf-topology.json` portable normalization before the cumulative canonical topology is rebuilt.
- Caches the recursive portable Windows Server analysis object once before writing its two JSON alias paths.
- Reduces the previously non-terminating Set 3 Build path to 31.86s in the qualification environment without dropping raw topology or HWIDs.
- Corrects the Set 2 normalized qualification summary field `InfParseFailureCount` from an erroneous 29 to the observed 0.
- Cumulative development view now contains 3 artifacts, 89 INF topology records and 356 INF×Server rows.
- PowerShell AST parse errors: 0; final stage-selected qualification results are PASS / exit 0.

## 0.4.1 — 2026-08-10

RDNA Combined real-artifact qualification and cumulative canonical-view hardening.

- Qualifies Adrenalin 23.11.1 RDNA Combined: 29 INF, 0 parse failures, 12 KMDF-declaring INF, no UMDF declarations observed.
- Adds `ArtifactRole=RDNACombined` before generic RDNA filename classification.
- Extends Graphics analysis-surface evidence to count `Display`, `Display2`, and numeric `DisplayN` roots; the supplied Combined artifact reports 16 display INF out of 29 total INF.
- Confirms the Combined artifact contains all 21 RDNA-only INF files with identical INF SHA-256 values plus eight additional `Display2/WT6A_INF` INF files.
- Records two embedded AMD Display Driver entries: 31.0.21905.1001 (`Display2/U0397033.INF`) and 31.0.22023.1014 (`Display/U0397406.INF`), with 18/18 manifest payload paths resolved.
- Rebuilds `all-releases-summary.json`, `inf-topology.json`, Windows Server applicability views, and device/HWID matrices from all retained canonical per-artifact JSON records.
- Prevents Set 2 analysis from dropping the previously retained Set 1 artifact from cumulative GitHub views.
- Cumulative qualified development view now contains two 23.11.1 artifacts, 50 INF topology records, 200 INF×Server rows, and 19,406 device/HWID×Server rows.
- AST parse errors: 0 under PowerShell 7.6.4. Test/Acquire/Extract/Inspect PASS in one final stage-selected run; Build PASS in a second stage-selected run against the same final script and inventory, both exit 0.

## 0.4.0 — 2026-08-10

Canonical per-artifact analysis and Windows Server projection milestone.

- Preserves full Manufacturer → TargetOSVersion → Models section → model/HWID topology instead of relying on flat HWID lists.
- Preserves raw decorations, empty Models sections, model descriptions, install sections, hardware IDs and compatible IDs.
- Adds a Server selector simulation for member-server ProductType 3 profiles on Windows Server 2016 / 2019 / 2022 / 2025.
- Separates `AsPublished` INF applicability from a non-mutating ProductType `1 -> 3` `ServerProjection`.
- Adds `NATIVE_CANDIDATE`, `PATCH_CANDIDATE`, `REVIEW_REQUIRED`, `NOT_APPLICABLE`, and `INDETERMINATE` static assessments; runtime compatibility remains `NotEstablished`.
- Separates documented and observed WDF reference values in Server profiles.
- Captures `AddService` service-name evidence in addition to ServiceBinary evidence.
- Adds ArtifactKey 1:1 canonical Raw Analysis JSON under `inventory/releases/<version>/`; this generalizes the Chipset version-1:1 concept for Graphics multi-artifact releases.
- Adds `inf-topology.json`, `windows-server-applicability.json`, `all-releases-summary.json`, server matrix CSV, device/HWID × Server CSV, and per-artifact Markdown reports.
- Keeps observed facts and derived analysis separate inside canonical driver records.
- Expands synthetic Server-analysis self-tests from 3 to 5, including ProductType projection, build floor, most-specific build section selection, explicit empty-section exclusion, and WDF split.
- Adrenalin 23.11.1 RDNA regression: 21 INF, 0 parse failures; canonical ArtifactKey record and 84 INF×Server analysis rows generated.

## 0.3.0 — 2026-08-10

Windows Server static-analysis milestone driven by real Adrenalin 23.11.1 RDNA qualification.

- Parses INF `[Manufacturer]` TargetOSVersion decorations instead of treating file discovery as sufficient compatibility evidence.
- Normalizes architecture, OS major/minor, ProductType, SuiteMask, BuildNumber, Models section, and section-level HWID counts.
- Projects INF targeting onto Windows Server 2016 / 2019 / 2022 / 2025 base-build profiles.
- Distinguishes workstation-only ProductType 1 from Windows Server ProductType 2 / 3 scopes.
- Adds conservative KMDF/UMDF published-reference analysis; newer-than-reference declarations become `ReviewRequired`, never a static hard failure.
- Preserves complete AMD `InstallManifest.json` condition objects as raw Embedded evidence.
- Adds dedicated `windows-server-compatibility-analysis.json/.csv/.md` outputs and keeps runtime compatibility explicitly `NotEstablished`.
- Adds three synthetic parser self-tests for workstation ProductType blocking, Server build gating, and KMDF 1.31 reference behavior.
- Real Set 1 qualification: Adrenalin 23.11.1 RDNA, 21 INF, 0 parse failures, all 5 selected stages PASS / exit 0.
- Key observed result: primary Display INF `u0397406.inf` is explicitly workstation-targeted by ProductType 1 and is therefore `Blocked` for all four Windows Server profiles in the unmodified-INF static model. Other companion INFs can remain candidates and are evaluated independently.

## 0.2.0 — 2026-08-09

Real-artifact hardening milestone based on user-supplied Adrenalin installers.

- Qualifies real Adrenalin 26.5.2 Polaris/Vega and 26.7.1 Main installer artifacts without executing AMD setup.
- Adds `-LocalInstallerPath` local-only acquisition mode and `Provided` artifact assessment support.
- Extends filename classification to real `amd-software-adrenalin-edition` / `amd-software-pro-edition` artifact names.
- Records 7-Zip archive probes, including observed SFX type/offset/physical/tail sizes.
- Adds modern Graphics analysis-surface detection (`Packages/Drivers` + display INF) and stops redundant nested MSI/helper-PE extraction once reached.
- Removes filename-only nested EXE traversal; nested EXEs must be statically recognized as actual containers.
- Adds semantic parsing of `Config/InstallManifest.json` while preserving it as Embedded evidence.
- Adds case-insensitive mapping from manifest package URLs to extracted payload files for cross-platform correctness.
- Adds INF `[Strings]` token resolution for WDF directives while retaining raw evidence.
- Adds driver component classification from `Packages/Drivers/<component>` path evidence.
- Real qualification result: 59 INF records, 27 KMDF-declaring INFs, 2 UMDF-declaring INFs, 0 parse failures; final PASS / exit 0.

### Real artifacts

- 26.5.2 Polaris/Vega: 19 INF; embedded display driver 31.0.21925.1001; KMDF observed 1.15.
- 26.7.1 Main: 40 INF; embedded display driver 32.0.31035.1003; KMDF observed 1.11 / 1.15 / 1.23.0 / 1.31; UMDF observed 2.23.0 / 2.33.0.
- Both EXEs are direct 7z SFX containers with archive offset 1,785,344 bytes and expose `Packages/Drivers/Display/WT6A_INF` after one static extraction.

Windows PowerShell 5.1, transition-era / Unified and PRO real-artifact qualification remain pending before GA.

## 0.1.0 — 2026-08-09

Initial AMD Graphics research-tool development milestone.

- Forks the accepted AMD Chipset Research Toolkit architecture into a sibling Graphics research tool without carrying over the Chipset accepted dataset.
- Adds Graphics release identity as `PackageFamily | Branch | ReleaseVersion`.
- Supports Adrenalin numeric versions and PRO Edition quarter-style versions.
- Preserves same-version Main and Polaris/Vega release identities separately.
- Adds multi-artifact acquisition so PRO Edition release notes can retain RDNA, Polaris/Vega and Windows Server artifacts independently.
- Adds `ArtifactKey` and artifact-role evidence throughout acquisition/extraction/inspection/build.
- Adds Graphics source-variant detection for `WT6A_INF`, `WT64A` and `U<digits>_INF` layouts.
- Retains static recursive 7-Zip and in-script InstallShield `ISSetupStream` extraction architecture; vendor installer execution is prohibited.
- Replaces Chipset-specific embedded `Info.xml` / `DevID.xml` assumptions with generic evidence-only manifest/XML/JSON recording.
- Build output retains all acquisitions/extractions per ReleaseKey instead of collapsing to one artifact.
- Hardens 7-Zip dependency probing so a broken wrapper is not accepted as a usable extractor.
- Adds bootstrap seeds covering Main, Polaris/Vega, Unified and PRO Edition research identities.

### Development qualification

- AST parser: 0 errors under PowerShell 7.6.4.
- Test stage: PASS / exit 0 with usable Debian 13 7-Zip 25.01 binary.
- Discovery harness: 10 seed identities retained with sitemap transport intentionally unavailable.
- Metadata harness: 1 Main + 1 Polaris/Vega + 3 PRO artifact URLs parsed as expected.
- Acquisition harness: 3 PRO-style artifacts classified as RDNA / PolarisVega / WindowsServer-PolarisVega.
- Nested synthetic package: ZIP -> ZIP -> `WT6A_INF` -> INF.
- Synthetic payload: HWID + KMDF 1.33 + UMDF 2.33.0 captured; Extract/Inspect/Build PASS / exit 0.

This is not a GA or accepted AMD historical baseline. Real AMD installer and Windows PowerShell 5.1 qualification remains pending.
## Rev48 common evidence-storage correction — 2026-08-19

- Unified Chipset, NPU, and Graphics final evidence under `private\evidence`.
- Added clear `EVIDENCE ZIP TO SHARE` output, SHA-256 companion files, and `LATEST-EVIDENCE.txt`.
- Added short raw run directories and `ZipOnly` / `ZipAndDirectory` retention.
- Blocked evidence destinations outside the canonical tool-local tree, including UNC, SUBST, and reparse-point routes.
- Removed the NPU TEMP emergency-evidence fallback.
- Corrected the Graphics empty-argument SUBST probe and made diagnostic failure fail-closed.
## 3.0.0 REV76 Gate 2G acceptance — 2026-08-22

- Accepts the exact REV75 bounded Windows Client Gate 2G and incorporates its
  complete 69-file generated public snapshot byte-for-byte.
- Keeps the root script, schemas and executable version unchanged.
- Defines only the remaining direct Windows Server `PathSafety,Test` smoke; no
  launcher or all-track rerun is added.
